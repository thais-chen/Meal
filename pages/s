    onAuthStateChanged(auth, (user) => {
        if (user) {
          // User is signed in, see docs for a list of available properties
          // https://firebase.google.com/docs/reference/js/firebase.User
          const uid = user.uid;
          setDoc(doc(db, uid, form), {
            name: newImg.label,
            image: newImg.image,
            timestamp: serverTimestamp(),
          });

        }
      });